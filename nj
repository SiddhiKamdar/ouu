INSERT INTO #temp
    SELECT 
        e.full_name,
        NULL,
        p.position_name,
        NULL,
        e.profile_picture_url,
        NULL
    FROM employees e
    JOIN positions p
        ON e.position_id = p.position_id
    WHERE e.employee_id = @employee_id;
